start <exo name>
    -> mdkir name
    -> echo '#include "name.hh" > name/name.c'
done
    -> ./make_header.c name.c

OPTIONAL git management
ask for commit msg
ask for tag end
git add .
git commit -m "msg"
git tag -ma tag
git push // if auto push
